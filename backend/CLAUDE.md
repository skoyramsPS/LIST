# CLAUDE.md

The backend contract lives in **`AGENTS.md`** at this directory root. It is the
single source of truth for all coding agents working in `backend/`.

**Read `AGENTS.md` now**, then follow its routing to `docs/architecture/index.md`.

Do not duplicate guardrails here — this file is intentionally a pointer only,
so the contract can never fork between agents.
